# 第 1 周任务卡（9.17 – 9.22）

## 本机环境扫描结果（2026.09.16）

| 项目 | 扫描时 | 现在 |
|---|---|---|
| Git 2.55.0 | 已装 | ✅ 沿用 |
| VS Code | 已装 | ✅ 沿用 |
| Python | 只有 WorkBuddy 内置的 3.13.14 | → 已换 conda 环境 Python **3.11.16** |
| Miniconda | 未装 | ✅ conda 26.7.1 → `C:\Users\18172\miniconda3` |
| numpy / pandas / matplotlib / scipy | 无 | ✅ 2.4.6 / 3.0.5 / 3.11.2 / 1.17.1 |
| xarray / netCDF4 / cartopy | 无 | ✅ 2026.7.0 / 1.7.4 / 0.25.0 |
| jupyter / metpy / cdsapi | 无 | ✅ 已装 |
| SSH key | 未生成 | ⬜ 待你生成 |
| GitHub / Copernicus CDS 账号 | 未注册 | ⬜ 待你注册 |

**环境已就绪**（2026.09.16 配好），只需：

```bash
conda activate ai-met
```

若提示 conda 不是命令，用完整路径：`C:\Users\18172\miniconda3\Scripts\conda.exe activate ai-met`
在 VS Code 里按 `Ctrl+Shift+P` → `Python: Select Interpreter` → 选 `C:\Users\18172\miniconda3\envs\ai-met\python.exe`

已验证可跑通：xarray 建 DataArray + cartopy 投影 + coastlines + gridlines → 出图 `week1_first_figure.png`。

**本周目标**：把环境搭起来，让 GitHub 出现第一个绿点，并画出第一张气象数据图。
不追求学多少，追求**每一天的产出都能留下痕迹**。

每天 2 小时（工作日）/ 3 小时（周末）。所有命令默认在 Windows 上执行。

---

## 开始前：一次性准备（9.17 优先做）

### 1. 装 Miniconda

> **2026.09.16 已由 Buddy 用清华镜像下载并静默安装到 `C:\Users\18172\miniconda3`，本节可跳过。**
> 若需自行重装：<https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/> 选 `Miniconda3-latest-Windows-x86_64.exe`
> 官方源 `repo.anaconda.com` 国内实测 8 分钟没下完，务必用镜像。

装完打开 Anaconda Prompt（或 Git Bash），换清华源加速：

```bash
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/
conda config --set show_channel_urls yes
```

创建环境（Python 3.11，兼容性最好）：

```bash
conda create -n ai-met python=3.11 -y
conda activate ai-met
conda install -c conda-forge numpy pandas matplotlib jupyter xarray netCDF4 -y
```

> **cartopy 别用 pip 装**，Windows 上会缺 GEOS / PROJ 依赖。等 Day 5 需要时再用 conda 装。

### 2. 装 Git 和 VS Code

- Git：<https://git-scm.com/download/win>，一路默认
- VS Code：<https://code.visualstudio.com/>，装 Python + Jupyter 两个扩展

配置身份：

```bash
git config --global user.name "ly"
git config --global user.email "你的邮箱"
```

### 3. 注册两个账号（今天就申请，审核要等）

- **GitHub**：<https://github.com>
- **Copernicus CDS（ERA5 数据源）**：<https://cds.climate.copernicus.eu/>
  用学校邮箱（@nuist.edu.cn）注册，通过更快，通常 1–2 个工作日。
  注册后到数据集页面点「同意许可协议」，否则下不了数据。

---

## 逐日任务

### Day 1 · 9.17 周四（2h）第一次提交

> 装机环节已完成，今天直接开始建仓库。

| 做什么 | 怎么做 |
|---|---|
| 建学习仓库 | `mkdir ai-met-journey && cd ai-met-journey && git init` |
| 写 README | 三行：我是谁、要做什么、这周做什么 |
| 推送到 GitHub | 在 GitHub 网页建同名空仓库，然后 `git remote add origin ... && git push -u origin main` |

**验收**：GitHub 主页出现**第一个绿点**。
**卡住就看**：Pro Git 中文版 <https://git-scm.com/book/zh/v2> 第 1–2 章。

### Day 2 · 9.18 周五（2h）命令行 + Python 自测

| 做什么 | 怎么做 |
|---|---|
| 练命令行 | 用 Git Bash：ls / cd / mkdir / cp / mv / rm / cat / grep / 管道 \| / 重定向 > |
| Python 自测 | 觉得基础不牢就看廖雪峰 Python 教程，够牢就直接做 NumPy 100 题 |
| NumPy 练习 | <https://github.com/rougier/numpy-100> 做前 30 题 |

**验收**：纯命令行完成「建目录 → 建文件 → 移动 → 查看内容」；
提交一个 `numpy_practice.py`，里面有用 NumPy 做的矩阵乘法和广播。

### Day 3 · 9.19 周六（3h）NumPy + Pandas

| 做什么 | 怎么做 |
|---|---|
| NumPy 剩下部分 | 广播机制、轴（axis）的概念、布尔索引 —— 这三个是后面天天用的 |
| Pandas 入门 | Series / DataFrame、read_csv、groupby、merge、pivot |
| 练手 | 找一份 CSV（Kaggle Titanic 就行）做一遍 describe 和分组统计 |

**验收**：一份 `pandas_practice.ipynb`，能不看文档写出 groupby + merge。

### Day 4 · 9.20 周日（3h）xarray 尝鲜

气象数据的核心工具，早点见到成果能续命。

```python
import xarray as xr
ds = xr.tutorial.open_dataset("air_temperature")
print(ds)                      # 看结构：dimensions / coordinates / variables
ds.air.isel(time=0).plot()     # 画第一张图
ds.air.sel(time="2013-01-01").mean(dim="time").plot()
```

弄懂三件事：`dim`、`coord`、`variable` 分别是什么；`sel` 和 `isel` 的区别。

**验收**：一张气温空间分布图，保存在 `week1/figures/`。

### Day 5 · 9.21 周一（2h）画得像样一点

```bash
conda install -c conda-forge cartopy -y
```

用 cartopy 加海岸线和经纬网，把 Day 4 的图重画一版：

```python
import cartopy.crs as ccrs
fig, ax = plt.subplots(subplot_kw={"projection": ccrs.PlateCarree()})
ds.air.isel(time=0).plot(ax=ax, transform=ccrs.PlateCarree())
ax.coastlines()
ax.gridlines(draw_labels=True)
```

**验收**：一张带海岸线的地图，能拿去发朋友圈那种。
**装不上就跳过**，用 matplotlib 画也行，别在环境上耗超过 1 小时。

### Day 6 · 9.22 周二（2h）收尾 + 申请数据

| 做什么 | 怎么做 |
|---|---|
| 确认 CDS 账号 | 收到激活邮件、已同意许可协议 |
| 配置 API key | 见下方 |
| 补完没做完的 | 优先补 Day 1–4 的产出 |
| 周复盘 | README 里写：本周做了什么、卡在哪、下周改什么 |

配置 CDS API（Windows）：

```bash
type nul > %USERPROFILE%\.cdsapirc
```

然后用记事本打开 `C:\Users\你的用户名\.cdsapirc`，写入：

```
url: https://cds.climate.copernicus.eu/api/v2
key: 你的UID:你的API密钥
```

key 在 CDS 网站右上角用户名 → Your profile → API key。

**验收**：GitHub 连续 6 天绿点；`week1/` 下有代码和图；CDS 账号可用。

---

## 网课与资料

### 本周用

| 资源 | 用途 | 链接 |
|---|---|---|
| 廖雪峰 Python 教程 | 中文，查漏补缺 | liaoxuefeng.com/books/python/introduction/index.html |
| Harvard CS50P | 英文，体系最扎实，有中文字幕 | cs50.harvard.edu/python |
| Pro Git 中文版 | Git 查字典 | git-scm.com/book/zh/v2 |
| NumPy 100 题 | 练习 | github.com/rougier/numpy-100 |

CS50P 不用全看。Python 基础够就直接跳过，把时间给 NumPy 和 xarray。

### 第 2 周开始

| 资源 | 用途 | 链接 |
|---|---|---|
| **Project Pythia** | 地球科学 Python 教科书（NCAR 出品），xarray / cartopy / MetPy / ML 章节齐全 | projectpythia.org |
| **Unidata Python Training** | 气象专属 Python 教程 | unidata.github.io/python-training |
| xarray 官方文档 | 查 API | docs.xarray.dev |

Project Pythia 是你接下来一年最重要的免费教材，比任何中文博客都靠谱。

### 第 3 周起

| 资源 | 用途 |
|---|---|
| 吴恩达 Machine Learning Specialization（Coursera，有中文字幕） | 机器学习主线 |
| 3Blue1Brown 线性代数的本质（B站有官方中文版） | 线代直觉，配合第 3 周 |
| 李沐《动手学深度学习》d2l.ai + B站「跟李沐学AI」 | 深度学习主线 |

### 长期备查

- MetPy 教程（气象计算）：unidata.github.io/MetPy/latest/tutorials
- Papers with Code · Weather Forecasting 榜单：追前沿
- 书：《Python for Data Analysis》（Wes McKinney）、周志华《机器学习》、李航《统计学习方法》

---

## Windows 特供避坑

1. **cartopy 必须用 conda 装**：`conda install -c conda-forge cartopy`，pip 装必错
2. **路径不要有中文和空格**：`D:\ai-met\` 而不是 `D:\我的项目\AI 气象\`
3. **换源**：conda 和 pip 都换清华源，否则慢到怀疑人生
4. **`.cdsapirc` 点开头**：资源管理器不让建，用命令行 `type nul > %USERPROFILE%\.cdsapirc`
5. **环境卡住别死磕**：超过 1 小时就去用 Colab（免费 GPU，还能直接跑 notebook）
6. **每天提交一次**：哪怕只改一行，绿点断了很难补
